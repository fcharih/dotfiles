{ pkgs, ... }:
{
  home.username = "fcharih";
  home.homeDirectory = {{ if eq .chezmoi.os "darwin" }}"/Users/fcharih"{{ else if eq .chezmoi.os "linux" }}"/home/fcharih"{{ else }}"/home/fcharih"{{ end }};
  home.stateVersion = "24.11";

  home.packages = with pkgs; [
    lib.splitString "\n" (builtins.readFile ./packages.txt)
  ];

  programs.home-manager.enable = true;
}
